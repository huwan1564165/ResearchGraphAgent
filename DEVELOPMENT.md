# ResearchGraph 开发文档

这份文档用于帮助自己理解 ResearchGraph 深度研究 Agent 的整体结构、数据流和当前开发边界。它不是完整的产品需求书，具体功能以需求文档和实际代码为准。

## 1. 项目要解决什么问题

ResearchGraph 接收一个复杂的研究问题，把它拆成可执行的子问题，然后搜索资料、保存来源、提取证据，最后生成一份带有准确引用的研究报告。

项目最重要的特点是可追溯：报告中的结论不能只来自模型的生成结果，而要能继续找到对应的证据和来源。

```text
用户问题
  ↓
子问题拆解与用户确认
  ↓
资料搜索
  ↓
来源保存
  ↓
证据提取
  ↓
观点与结论整理
  ↓
带引用的研究报告
  ↓
结论 → 证据 → 来源 回溯
```

## 2. 第一版先实现什么

第一版先完成一个可以运行的最小闭环：

- 用户输入研究问题，并补充时间、地区、研究对象和关注重点。
- Agent 生成一组互不重复的子问题。
- 用户可以修改、删除、添加并确认子问题。
- 系统根据已确认的子问题搜索资料并保存来源信息。
- 系统从来源中提取证据，并记录证据支持或反驳的观点。
- 系统生成带引用的报告，区分事实、推测和综合判断。
- 用户可以从主要结论回溯到证据和来源。
- 系统记录搜索、提取和判断过程。

来源质量评分、复杂冲突归因、多 Agent、自动事实核查和报告版本对比先作为后续扩展，不放进第一轮实现。

## 3. 整体架构

第一版采用单体应用，使用一个协调 Agent 串起流程。模块按职责分开，但暂时不拆成多个独立 Agent。

```text
网页界面
  ↓
FastAPI 路由
  ↓
Coordinator 协调器
  ├── Decomposition   问题拆解
  ├── Researcher      搜索和来源整理
  ├── Evidence        证据提取
  └── Report          报告和引用生成
        ↓
Tools 工具层
  ├── SearchProvider   搜索服务适配器
  ├── SourceReader     来源内容读取
  ├── LLMClient        大模型调用
  ├── Citation         引用检查
  └── Storage          SQLite 数据访问
        ↓
SQLite 数据库
```

各层的职责如下：

- `web` 只处理页面展示、用户输入和请求响应。
- `agent` 负责研究流程和模型提示词，不直接处理数据库细节。
- `tools` 负责外部服务、文本读取、引用校验和数据存取。
- `models` 描述项目、问题、来源、证据、结论和日志等核心对象。
- `data` 保存本地 SQLite 数据库，方便演示和查看。

这种分层可以让搜索服务或大模型更换时，不必重写研究流程。

## 4. 主要数据对象

### Project 研究项目

保存一次完整研究任务，包括研究问题、时间范围、地区、研究对象、关注重点和当前状态。

### Question 子问题

保存总问题和拆解后的子问题。每个子问题需要有顺序、编辑状态和确认状态，只有确认后的子问题才进入搜索阶段。

### Source 来源

保存标题、作者、机构、发布时间、链接、摘要、来源类型和读取时间。后续可以在这里增加权威性、研究方法、相关性和利益关系等评价字段。

### Evidence 证据

保存来源中的原文摘录、位置、证据类型、支持或反驳关系、证据强度和不确定性。证据必须关联到具体来源和子问题。

### Claim 观点或结论

保存报告中的观点或结论，并标记为事实、推测或 Agent 综合判断。每个主要结论都应关联一个或多个证据。

### Report 研究报告

保存生成后的报告正文、版本号和生成时间。报告里的引用使用系统分配的来源 ID 或证据 ID，避免模型自行编造链接。

### ResearchLog 研究过程记录

记录 Agent 做过的搜索、读取、提取和判断，包括输入、输出、时间和判断理由。它用于解释系统是怎样得到最终结果的。

## 5. 一次研究任务如何运行

1. 用户创建项目并填写研究范围。
2. 协调器调用问题拆解模块，要求模型返回结构化子问题。
3. 用户在页面上修改子问题并确认。
4. 协调器逐个调用搜索适配器，保存并去重来源。
5. 来源读取器获取摘要或正文，证据模块提取可引用片段。
6. 报告模块只使用已保存的来源和证据生成报告。
7. 引用检查器确认报告中的引用都能在数据库中找到。
8. 用户点击结论时，系统沿着 `Claim → Evidence → Source` 返回证据链。

如果证据不足、来源互相矛盾或无法确认结论，系统应在报告中明确标注不确定性，而不是补写一个确定答案。

## 6. 外部服务的处理方式

搜索和大模型调用都使用适配器接口：

- 开发和测试阶段使用确定性的演示数据，保证没有 API 密钥时也能运行。
- 论文搜索可以接入 Semantic Scholar 或 Crossref。
- 网页和报告搜索通过独立的 `WebSearchProvider` 接入。
- 更换搜索服务时，只替换适配器，不改变 `Researcher` 的调用方式。

这样做的目的，是把网络不稳定、API 限额和模型差异控制在工具层，同时让测试保持可重复。

## 9. 当前验证状态

当前版本已完成 `.env` 配置读取、OpenAI 兼容接口接入、真实学术搜索以及 Semantic Scholar 限流后回退到 Crossref 的处理。已通过配置的 OpenAI 兼容网关完成真实请求、问题拆解和离线完整流程验证。LLM 证据抽取返回空结果或调用失败时，会自动回退到规则式抽取，避免单个来源导致整个研究流程没有证据。没有 API Key 时，系统仍会使用规则式问题拆解、证据抽取和结论综合。

## 7. 推荐项目目录

```text
ResearchGraph/
├── app.py                         # FastAPI 启动入口
├── config.py                      # 配置和环境变量
├── requirements.txt               # Python 依赖
├── README.md                      # 启动、演示和使用说明
├── DEVELOPMENT.md                 # 本开发文档
├── .env.example                   # API 配置示例
│
├── agent/
│   ├── coordinator.py             # 协调完整研究流程
│   ├── decomposition.py           # 复杂问题拆解
│   ├── researcher.py              # 搜索和来源整理
│   ├── evidence.py                # 证据提取和关联
│   ├── report.py                  # 报告生成和引用检查
│   └── conflict.py                # 后续冲突分析接口
│
├── models/
│   ├── project.py                 # 研究项目
│   ├── question.py                # 总问题和子问题
│   ├── source.py                  # 来源
│   ├── evidence.py                # 证据
│   ├── claim.py                   # 观点和结论
│   ├── report.py                  # 报告
│   └── research_log.py            # 研究过程记录
│
├── tools/
│   ├── storage.py                 # SQLite 数据访问
│   ├── search.py                  # 搜索服务适配器
│   ├── source_reader.py           # 来源读取
│   ├── llm.py                     # 大模型调用封装
│   └── citation.py                # 引用合法性检查
│
├── web/
│   ├── routes.py                  # 页面和 API 路由
│   ├── templates/                 # HTML 模板
│   └── static/                    # CSS 和 JavaScript
│
├── data/
│   └── researchgraph.db           # SQLite 数据库
│
└── tests/
    ├── test_development_doc.py    # 开发文档完整性检查
    ├── test_decomposition.py      # 子问题拆解
    ├── test_storage.py            # 数据保存和查询
    ├── test_search.py             # 来源去重和字段标准化
    ├── test_evidence_trace.py     # 结论到来源的回溯
    ├── test_report_citations.py   # 报告引用检查
    └── test_demo_flow.py           # 推荐题目的端到端流程
```

## 8. 当前阶段的开发顺序

每个阶段都单独编写或更新测试，并创建对应 Git commit：

1. 项目骨架、配置、SQLite 数据模型和基础测试。
2. 研究问题输入、子问题生成、子问题编辑和确认。
3. 搜索适配器、来源保存、链接去重和搜索日志。
4. 证据提取、证据与子问题及观点的关联。
5. 报告生成、引用合法性检查和不确定性标注。
6. 结论回溯页面和端到端演示。
7. 使用老师推荐的问题完成一次完整演示，并补充 README。

每完成一个阶段，都要运行相关测试，再检查 Git diff 和提交记录。这样可以让每一步都能单独回滚，也方便向老师展示项目是如何逐步完成的。

## 9. 推荐演示题目

```text
大语言模型是否能够提升中学生的学习效果？
重点分析数学和英语学习，关注 2020 年之后的研究，
并说明短期成绩提升与长期知识保持之间的差异。
```

演示时重点展示三件事：用户如何确认子问题、报告中的结论如何引用证据，以及点击结论后如何回到原始来源。

## 10. 第一版完成标准

第一版完成后，至少应能做到：

- 启动应用并创建一个研究项目。
- 生成并编辑子问题，然后确认研究范围。
- 搜索或加载一批来源并保存到 SQLite。
- 从来源生成带定位信息的证据记录。
- 生成带有效引用的报告。
- 从报告主要结论回溯到证据和来源。
- 在证据不足时显示明确的不确定性说明。
- 测试通过，且每个开发阶段都有对应 Git commit。

